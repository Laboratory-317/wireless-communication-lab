# Source priority for future project work

When using this archive as project knowledge:

1. Prefer `synthesis/PROJECT_SNAPSHOT.md` for the consolidated current model.
2. Use `repository-docs/AGENTS.md` for agent operating rules.
3. Use `repository-docs/ARCHITECTURE.md` for stable structure and build flow.
4. Use `repository-docs/STATE.md` for known issues and current/last tracked work.
5. Use the remaining `repository-docs/` files for specialized details.
6. Use `synthesis/OPENAI_CODEX_GUIDANCE.md` for why the documentation is structured this way.
7. Treat `historical/Hand-off.txt` as historical background only.

If you have live access to the GitHub repository later, the live `main` branch
takes precedence over this dated archive.
